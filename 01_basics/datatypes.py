#  There are following datatypes in python:

# numbers : used to store the numbers. 
# decimal() and fraction() are also used to store the decimal and fraction stuff.

# Strings : both double and single quotes can be used here.

# list : A list in Python is an ordered, mutable collection that can store elements of different data types.

# range is a function which generates the numbers based on the arguments given to it. now i depends on us 
# what we have to do with those numbers we can run a loop based on that store them on the list etc etc.
# one of the reason for using the range is that we dont have incriment operators for the variables in the python 
# so we created range which creates number on the go the other way to do is using a list which is not really memory efficient. 

# A tuple in Python is an ordered, immutable collection of elements that can store multiple data types.

# we can also work on the files with the python directly we using the files datatype in the python.

 
# we should make sure we add two variables with the same datatypes otherwise it might cause confusion 
